# Express API proxy port (default: 3001)
PORT=3001

# Comma-separated browser origins allowed by CORS in production.
# Leave empty in development to allow all origins.
# Example: https://your-app.example.com,http://localhost:5173
CORS_ORIGIN=
